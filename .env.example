# =============================================================================
# TTS OpenAI Wrappers - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# API Authentication (optional)
# -----------------------------------------------------------------------------
# API key for authenticating requests to the TTS API
# If not set, the API will be open (no authentication required)
TTS_API_KEY=

# -----------------------------------------------------------------------------
# TTS Engine Configuration
# -----------------------------------------------------------------------------
# Which TTS engine to use: kokoro | cosyvoice | fishspeech
TTS_ENGINE=kokoro

# Comma-separated list of voices to preload, or "all" or "none"
TTS_PRELOAD_VOICES=none

# Maximum text length allowed (characters)
TTS_MAX_TEXT_LENGTH=10000

# -----------------------------------------------------------------------------
# HuggingFace (required for some models)
# -----------------------------------------------------------------------------
# HuggingFace API token for downloading gated models
# Required for: Fish Speech (openaudio-s1-mini), CosyVoice
# Get your token at: https://huggingface.co/settings/tokens
HF_TOKEN=

# -----------------------------------------------------------------------------
# RunPod Deployment
# -----------------------------------------------------------------------------
# RunPod API key for deploying serverless endpoints
# Get your key at: https://www.runpod.io/console/user/settings
RUNPOD_API_KEY=

# -----------------------------------------------------------------------------
# GitHub Container Registry (for CI/CD)
# -----------------------------------------------------------------------------
# GitHub Personal Access Token with packages:read and packages:write scopes
# Used by GitHub Actions to push Docker images to ghcr.io
# This is typically set as a repository secret, not in .env
GHCR_TOKEN=

# -----------------------------------------------------------------------------
# Engine-Specific Configuration
# -----------------------------------------------------------------------------

# --- Fish Speech ---
# Internal API URL (usually localhost when running in container)
FISH_API_URL=http://127.0.0.1:8080

# --- CosyVoice ---
# (No additional configuration required)

# --- Kokoro ---
# (No additional configuration required)

# -----------------------------------------------------------------------------
# Development / Local Testing
# -----------------------------------------------------------------------------
# Server host and port for local development
HOST=0.0.0.0
PORT=8000

# Log level: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Enable debug mode (more verbose output)
DEBUG=false
